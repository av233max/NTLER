MuOpenControlDevice

返回控制句柄，失败为0

MuQueryLebBase

返回加载器参数的内存指针，失败为0
之后往里面写入三个参数，再创建进程，就能启动特定语言环境

MuAddAppConfig

添加一个记录，MU_APPLICATION_CONFIGURATION的结构在下面说明
成功为bool true，失败为false

MuEnumAppConfig

获取记录列表，返回值是写入的数据数。如果为0，表示失败
如果写入数据数比EnumConfig.RequiredBufferSize小，表示需要更大的内存来装入列表
需要分配EnumConfig.RequiredBufferSize个字节并重新调用API
BufferSize至少大于一个EnumConfig结构，第一次调用，用来获得RequiredBufferSize
如果第一次调用成功返回，表示列表为空

MU_APPLICATION_CONFIGURATION
12个字节存放加载器参数
2~520个字节用来存放路径

作为参数传递时，大小需要加上末尾的空字符

在EnumConfig中，MU_APPLICATION_CONFIGURATION的起始部分为4字节对齐